Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
psycopg[binary]==3.2.3
gunicorn==23.0.0
whitenoise==6.8.2
python-decouple==3.8
requests==2.32.3
Pillow==11.0.0
django-storages==1.14.4
django-filter==24.3
drf-spectacular==0.27.2
django-htmx==1.21.0
boto3==1.35.84

# Skin AI: age estimation via DeepFace + texture metrics in Pillow/numpy.
# DeepFace 0.0.93 hard-imports cv2 at package load (image_utils.py), so
# opencv-python-headless has to stay installed even though our own
# pipeline doesn't call cv2 directly. The Dockerfile installs the
# matching system libs (libxcb1, libgl1, libglib2.0-0).
opencv-python-headless==4.10.0.84
tf-keras==2.18.0
deepface==0.0.93
# Face landmark detection — feeds apps/skinai/landmarks.py to crop
# scans to the actual face region and isolate the under-eye area for
# dark-circles + eyebags scoring. ~85 MB; the only large dep we
# carry on top of TF/cv2 but the accuracy lift is significant. Pinned
# because the API shape (mp.solutions.face_mesh) is stable in 0.10.x.
mediapipe==0.10.18

# Error reporting. Activates when the SENTRY_DSN env var is set;
# silent no-op otherwise so dev runs don't need a real DSN.
sentry-sdk[django]==2.18.0
