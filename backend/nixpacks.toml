[phases.setup]
nixPkgs = ["python312", "gcc", "pkg-config", "libpq", "openssl"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "./start.sh"
